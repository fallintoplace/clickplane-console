# Keep fixture mode for an offline demo. Use clickhouse mode with `docker compose up -d`.
CLICKPLANE_EXECUTOR=fixture
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_USERNAME=default
CLICKHOUSE_PASSWORD=clickplane-local
CLICKHOUSE_DATABASE=default
CLICKHOUSE_ENABLE_KILL_QUERY=true
